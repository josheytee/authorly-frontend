import React from "react";

const Home = () => {
    return (
        <div>
            <h1>Welcome to the Home Page</h1>
        </div>
    );
};

export default Home; // Ensure that you are exporting the component as default
